"use client";

import React from 'react';
import { Checkbox } from '@/components/ui/Checkbox';
import { Button } from '@/components/ui/button';
import { Slider } from '@/components/ui/slider';

interface FilterSidebarProps {
    className?: string;
}

export const FilterSidebar: React.FC<FilterSidebarProps> = ({ className }) => {
    return (
        <div className={`bg-white rounded-[24px] p-6 shadow-sm border border-neutral-100 ${className}`}>
            <div className="flex items-center justify-between mb-6">
                <h3 className="text-xl font-bold text-neutral-900 font-display">Filters</h3>
                <button className="text-sm text-primary font-medium hover:underline">Reset all</button>
            </div>

            <div className="space-y-8">
                {/* Work Schedule */}
                <div>
                    <h4 className="text-base font-bold text-neutral-900 mb-4">Work schedule</h4>
                    <div className="space-y-3">
                        <Checkbox label="Full-time" />
                        <Checkbox label="Part-time" />
                        <Checkbox label="Internship" />
                        <Checkbox label="Project work" />
                        <Checkbox label="Volunteering" />
                    </div>
                </div>

                {/* Salary Range */}
                <div>
                    <h4 className="text-base font-bold text-neutral-900 mb-4">Salary range</h4>
                    <div className="px-2">
                        {/* Placeholder for a chart/graph if needed, for now just slider */}
                        <div className="h-12 flex items-end gap-1 mb-2">
                            {[20, 40, 60, 30, 70, 50, 80, 40, 60, 30, 50, 20, 40, 60, 80, 50, 30, 40, 20, 10].map((h, i) => (
                                <div key={i} className="flex-1 bg-neutral-100 rounded-t-sm" style={{ height: `${h}%` }}></div>
                            ))}
                        </div>
                        <Slider defaultValue={[200, 1000]} max={2000} step={50} className="mb-4" />
                        <div className="flex items-center gap-4">
                            <div className="flex-1 bg-white border border-neutral-200 rounded-xl px-3 py-2 text-sm font-medium text-neutral-700">
                                ₹ 200
                            </div>
                            <div className="flex-1 bg-white border border-neutral-200 rounded-xl px-3 py-2 text-sm font-medium text-neutral-700">
                                ₹ 1,000
                            </div>
                        </div>
                    </div>
                </div>

                {/* Employment Type */}
                <div>
                    <h4 className="text-base font-bold text-neutral-900 mb-4">Employment type</h4>
                    <div className="space-y-3">
                        <Checkbox label="Full day" />
                        <Checkbox label="Flexible schedule" />
                        <Checkbox label="Shift work" />
                        <Checkbox label="Distant work" />
                    </div>
                </div>

                {/* Work Style */}
                <div>
                    <h4 className="text-base font-bold text-neutral-900 mb-4">Work style</h4>
                    <div className="space-y-3">
                        <Checkbox label="Office" />
                        <Checkbox label="Hybrid" />
                        <Checkbox label="Remote" />
                    </div>
                </div>
            </div>
        </div>
    );
};
